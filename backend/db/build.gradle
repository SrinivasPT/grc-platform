// db module — Liquibase changelog files only
// Migrations are applied via:
//   docker run liquibase/liquibase:4.28 update  (local dev)
//   Spring Boot startup (Liquibase auto-runs via platform-api application.yml)
